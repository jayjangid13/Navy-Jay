restrict_login_ip.settings:
  type: config_object
  label: 'Restrict Login IP Settings'
  mapping:
    ip_ranges:
      type: string
      label: 'Allowed IP Ranges'
      description: 'Semicolon-separated list of allowed IP ranges in CIDR format.'
